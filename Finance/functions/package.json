{
  "name": "turnos-omnia-functions",
  "private": true,
  "engines": { "node": "20" },
  "main": "src/index.js",
  "scripts": {
    "lint": "node --check src/index.js && node --check scripts/set-user-role.js",
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^14.4.0",
    "firebase-functions": "^7.4.0"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "eslint-config-google": "^0.14.0"
  }
}
